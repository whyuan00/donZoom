import { SafeAreaView,Text } from "react-native"

const ReportTabScreen = ()=>{
    return (
      <SafeAreaView>
        <Text>unsafe</Text>
      </SafeAreaView>
    );
}

export default ReportTabScreen;